import React from 'react';
import PageReplacementSimulator from './components/PageReplacementSimulator';

// No Material UI needed - the component has its own styling with Tailwind
function App() {
  return <PageReplacementSimulator />;
}

export default App;